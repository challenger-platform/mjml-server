{
  "type": "module",
  "name": "mjml-server",
  "version": "1.0.2",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "prod": "node src/server.js",
    "dev": "docker compose up --remove-orphans --force-recreate",
    "build": "docker compose -f docker-compose.prod.yml build",
    "test-prod": "docker compose -f docker-compose.prod.yml up --remove-orphans --force-recreate",
    "push": "docker compose -f docker-compose.prod.yml push"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fastify": "^5.12.5",
    "handlebars": "^4.7.7",
    "mjml": "^5.4.1",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "eslint": "^10.0.2",
    "nodemon": "^3.1.14"
  }
}
